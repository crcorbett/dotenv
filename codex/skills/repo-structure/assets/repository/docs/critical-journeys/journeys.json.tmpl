{
  "$schema": "../../tools/governance/schemas/critical-journeys.schema.json",
  "version": 1,
  "journeys": [
    {
      "id": "WEB-HOME",
      "actorOrConsumer": "Browser user",
      "boundary": "TanStack Start home route",
      "startingState": "A locally bootstrapped repository with the web app available.",
      "inputs": ["GET /"],
      "stepsOrProcedureOwner": "docs/architecture/testing-and-quality.md",
      "expectedBehavior": ["The application renders its home route without a fatal error."],
      "expectedSideEffects": ["No unexpected durable side effect."],
      "preservedInvariants": ["Effect execution remains application-owned."],
      "oracle": "The rendered page contains the expected home content and no fatal runtime error.",
      "environment": "local",
      "authority": "none",
      "evidence": ["docs/proof/"],
      "nonClaims": ["Local proof does not establish a deployed provider route."]
    },
    {
      "id": "HTTP-ITEM-LIST",
      "actorOrConsumer": "HTTP API consumer",
      "boundary": "HTTP item-list endpoint",
      "startingState": "The domain and HTTP API packages are available in-process.",
      "inputs": ["GET /api/items"],
      "stepsOrProcedureOwner": "docs/architecture/testing-and-quality.md",
      "expectedBehavior": ["The HTTP boundary returns a Schema-decoded domain item list."],
      "expectedSideEffects": ["No unexpected durable side effect."],
      "preservedInvariants": ["Unknown ingress decodes at the HTTP boundary."],
      "oracle": "The response passes the owning encoded response Schema and the invalid fixture fails closed.",
      "environment": "local in-process transport",
      "authority": "none",
      "evidence": ["docs/proof/"],
      "nonClaims": ["In-process transport proof does not establish a deployed public route."]
    },
    {
      "id": "RPC-ITEM-LIST",
      "actorOrConsumer": "RPC consumer",
      "boundary": "RPC item-list operation",
      "startingState": "The domain and RPC packages are available in-process.",
      "inputs": ["ListItems RPC request"],
      "stepsOrProcedureOwner": "docs/architecture/testing-and-quality.md",
      "expectedBehavior": ["The RPC client invokes the Schema-owned operation through its handler Layer."],
      "expectedSideEffects": ["No unexpected durable side effect."],
      "preservedInvariants": ["The handler delegates to the domain service without a raw client escape hatch."],
      "oracle": "The client result passes the owning response Schema and the invalid fixture fails closed.",
      "environment": "local in-process RPC",
      "authority": "none",
      "evidence": ["docs/proof/"],
      "nonClaims": ["In-process RPC proof does not establish deployed network reachability."]
    }
  ]
}
